# Photographs ARE committed — GitHub Pages serves the repository contents, so an
# ignored image is an image the deployed site cannot show.
#
# Populate this folder with tools/fetch-images.py, then commit the results
# together with CREDITS.md, which carries the attribution those licences require.
